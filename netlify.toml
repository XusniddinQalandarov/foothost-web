# Next.js on Netlify uses the OpenNext adapter automatically (no plugin required for Next ≥ 13.5).
# https://docs.netlify.com/build/frameworks/framework-setup-guides/nextjs/overview/

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"
